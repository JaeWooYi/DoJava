package sec14_modifier.a1_accessModifier.ex02;

public class a01_AccessModifierOfClass_1 {
    // 1. 객체 생성
    ABC abc = new ABC();    // 객체 생성 가능(ABC abc) + 생성자 호출 가능(new ABC())
    BCD bcd = new BCD();    // 객체 생성 가능(BCD bcd ) + 생성자 호출 가능(new BCD())
    CDE cde = new CDE();    // 객체 생성 가능(CDE cde) + 생성자 호출 가능(new CDE())
}
